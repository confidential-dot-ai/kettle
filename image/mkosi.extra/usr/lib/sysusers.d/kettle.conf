# Service identity baked into the measured account database. IDs are pinned so
# a base-image change cannot shift them, and bin/image-build fails the build if
# the applied IDs differ from these.
#Type Name       ID       GECOS                  Home          Shell
g     kettle     900      -                      -             -
g     sev-guest  901      -                      -             -
u     kettle     900:900  "Kettle build server"  /home/kettle  /bin/bash
m     kettle     sev-guest
